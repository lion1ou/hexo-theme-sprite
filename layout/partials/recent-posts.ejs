
<% if (page.posts.length > 0) { %>
  <div class="recent-posts">
    <% page.posts.sort('date', -1).each(function(post) { %>

      <article class="recent-post-item">
        <a class="title" href="<%=config.root + post.path%>"><h2><%=post.title%></h2></a>
        <div class="post-meta">
          <% if (theme.postItem.showDate && post.date) { %>
            <time class="time" datetime="<%=post.date.toJSON()%>">
              <%=date(post.date)%>
            </time>
          <% } %>

          
          <% if(theme.postItem.wordCount && theme.postItem.showDate) {%>
            <span class="slash">/</span>
          <% } %>
          <% if (theme.postItem.wordCount) { %>
            <span class="read">
              字数 <span class="post-count"><%= wordcount(post.content) %></span>
            </span>
          <% } %>
         
          <% if(theme.postItem.min2read && theme.postItem.wordCount) {%>
            <span class="slash">/</span>
          <% } %>
          <% if (theme.postItem.min2read) { %>
            <span class="read">
              <span class="post-count"><%= min2read(post.content) %> min</span> 读完
            </span>
          <% } %>

          <% if(theme.postItem.wordCount && theme.postItem.showTags) {%>
            <span class="slash">/</span>
          <% } %>
          
          <% if (theme.postItem.showTags && post.tags) { %>
            <%- list_tags(post.tags, {
              show_count: false,
              class: 'index-tag'
            }) %>
          <% } %>
        </div>

        <% if (theme.postItem.showExcerpt && post.excerpt) { %>
          <div class="post-excerpt">
            <%- post.excerpt %>
          </div>
        <% } %>
        <a class="more" href="<%=config.root + post.path%>"> 阅读全文 > </a>
      </article>
    <% }); %>
  </div>
<% } %>